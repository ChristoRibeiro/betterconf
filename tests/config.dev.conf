env = "development"

path = ${PATH}

nodeEnv = ${NODE_ENV}

database {
  provider = "sqlite"
  url      = "file:./dev.db"
}

region = 2
regions = {
  1 = us-east-1
  2 = eu-west-1
  3 = sa-east-1
}

regionName = ${regions.${region}}

